# PRD: 대한민국 시도별 미세먼지 정보 서비스

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-08-19 |
| 작성자 | (담당자명) |
| 문서 목적 | 개인 프로젝트/포트폴리오용 서비스 기획 및 개발 기준 문서 |
| 상태 | Draft |

---

## 1. 개요 (Overview)

### 1.1 배경
미세먼지(PM10)와 초미세먼지(PM2.5)는 대한민국 국민의 일상적인 건강 관심사이며, 외출 계획 수립 시 필수적으로 확인하는 정보다. 기존 포털/앱들은 정보가 과도하게 많거나(날씨 앱에 부가기능으로 묻혀 있음), 반대로 특정 지역 검색이 번거로운 경우가 많다. 본 서비스는 **"도시를 칩(Chip) 형태로 클릭하면 즉시 해당 지역의 미세먼지 현황을 확인할 수 있는"** 가장 빠르고 직관적인 조회 경험을 제공하는 것을 목표로 한다.

### 1.2 한 줄 정의
> 전국 17개 시/도 칩을 클릭하면 실시간 미세먼지 농도·등급·예보·건강 행동요령을 한 화면에서 확인할 수 있는 반응형 웹 서비스

### 1.3 데이터 소스
- **제공처**: 한국환경공단(에어코리아) — 공공데이터포털(data.go.kr)
- **API명**: `한국환경공단_에어코리아_대기오염정보` (서비스명: ArpltnInforInqireSvc)
- **활용 기능**: 시도별 실시간 측정정보 조회(`getCtprvnRltmMesureDnsty`), 대기질 예보통보 조회(`getMinuDustFrcstDspth`)
- **인증**: 공공데이터포털 발급 서비스키(Encoding/Decoding key) 필요, 개발계정 기준 일 트래픽 500회 제한 → 캐싱 전략 필수

---

## 2. 목표 및 성공지표 (Goals & KPI)

### 2.1 프로덕트 목표
- 사용자가 **3초 이내**에 원하는 지역의 미세먼지 상태를 파악하도록 한다.
- 클릭 한 번(도시 칩 선택)으로 상세 정보에 도달하는 **Zero-Depth Navigation**을 구현한다.

### 2.2 성공지표 (포트폴리오/개인 프로젝트 기준 정성+정량 혼합)
| 지표 | 목표치 | 측정 방법 |
|---|---|---|
| 최초 콘텐츠 표시 시간(LCP) | 2.5초 이하 | Lighthouse |
| 칩 클릭 → 데이터 표시 시간 | 500ms 이하 (캐시 히트 기준) | 프론트엔드 성능 로그 |
| API 호출 실패 시 정상 폴백률 | 100% (에러 UI 노출) | QA 테스트 |
| 모바일/PC 반응형 대응 | 360px~1920px 전 구간 정상 렌더링 | 크로스 디바이스 테스트 |

---

## 3. 타겟 사용자

| 페르소나 | 설명 | 니즈 |
|---|---|---|
| 일반 사용자 | 출퇴근/등하교 전 날씨 겸 미세먼지를 빠르게 확인하는 20~50대 | 빠른 조회, 직관적 등급 표시 |
| 건강 취약계층/보호자 | 호흡기 질환자, 어린이/노약자 보호자 | 건강 행동요령, 등급별 권고사항 강조 |
| (2차) 채용/포트폴리오 심사자 | 서비스를 통해 기획·개발 역량을 평가 | 완성도 높은 UI/UX, 명확한 정보구조 |

---

## 4. 범위 (Scope)

### 4.1 In Scope (v1.0)
- 전국 **17개 시/도** 단위 칩 리스트 UI (서울·부산·대구·인천·광주·대전·울산·세종·경기·강원·충북·충남·전북·전남·경북·경남·제주)
- 칩 클릭 시 해당 시/도의:
  - PM10, PM2.5 실시간 농도(㎍/㎥)
  - 통합대기환경등급(좋음/보통/나쁨/매우나쁨) 색상 코딩
  - 오늘/내일/모레 대기질 예보
  - 등급별 건강 행동요령(외출 자제, 마스크 착용 권고 등)
  - 데이터 측정 시각(최종 갱신 시각) 표시
- 즐겨찾기 기능 (로컬 저장 기반, 최대 N개 도시 고정)
- 반응형 웹 (모바일/PC 겸용)
- API 실패/지연 시 에러 및 로딩 상태 UI

### 4.2 Out of Scope (v1.0, 향후 검토)
- 지도 기반 시각화 UI
- 시/군/구 단위 세분화
- 회원가입/로그인, 서버 기반 즐겨찾기 동기화
- 푸시/이메일 알림
- 다국어 지원
- 네이티브 모바일 앱

---

## 5. 사용자 스토리 (User Stories)

1. **As a** 사용자, **I want to** 첫 화면에서 전국 도시 칩 목록을 바로 보고 싶다 **so that** 별도 검색 없이 원하는 지역을 즉시 클릭할 수 있다.
2. **As a** 사용자, **I want to** 클릭한 도시의 미세먼지 등급을 색상으로 직관적으로 파악하고 싶다 **so that** 수치를 해석하는 수고 없이 빠르게 상황을 이해할 수 있다.
3. **As a** 건강 취약계층 보호자, **I want to** 현재 등급에 맞는 행동요령을 보고 싶다 **so that** 외출 여부를 판단할 수 있다.
4. **As a** 재방문 사용자, **I want to** 자주 보는 도시를 즐겨찾기에 등록하고 싶다 **so that** 매번 리스트에서 찾지 않아도 된다.
5. **As a** 사용자, **I want to** 데이터가 언제 측정된 것인지 알고 싶다 **so that** 정보의 신뢰도를 판단할 수 있다.

---

## 6. 기능 요구사항 (Functional Requirements)

### 6.1 도시 칩 리스트 (FR-01)
- 17개 시/도를 칩(Chip/Tab) 컴포넌트로 최상단에 가로 스크롤 또는 그리드로 배치
- 즐겨찾기한 도시는 리스트 최상단에 고정 노출 (별도 섹션 or 아이콘 표시)
- 현재 선택된 칩은 시각적으로 강조(active state)
- 기본 선택값: 최근 조회 도시 → 없으면 '서울'

### 6.2 미세먼지 상세 정보 패널 (FR-02)
칩 클릭 시 하단/우측(반응형에 따라 위치 변경)에 상세 패널 표시. 포함 요소:
- 도시명 + 측정 기준 시각(예: "14:00 기준")
- PM10 농도(㎍/㎥) + 등급 배지
- PM2.5 농도(㎍/㎥) + 등급 배지
- 통합대기환경등급 종합 표시 (좋음/보통/나쁨/매우나쁨)
- 등급별 색상 코딩 (아래 9.2 기준표 참조)
- 오늘/내일/모레 예보 카드 3개
- 등급별 건강 행동요령 텍스트 블록

### 6.3 즐겨찾기 (FR-03)
- 도시 칩 우측 상단 별(★) 아이콘 클릭으로 즐겨찾기 등록/해제
- `localStorage` 기반 저장 (로그인 불필요)
- 즐겨찾기 도시는 칩 리스트 최상단 별도 그룹으로 노출

### 6.4 로딩/에러 처리 (FR-04)
- API 응답 대기 중 스켈레톤 UI 표시
- API 실패/타임아웃 시 "일시적으로 데이터를 불러올 수 없습니다" 메시지 + 재시도 버튼
- 측정소 점검 등으로 결측치(`-`) 수신 시 "측정 데이터 없음" 별도 표시 (0으로 오인하지 않도록)

### 6.5 데이터 캐싱 (FR-05)
- 동일 도시 재조회 시 서버/클라이언트 캐시(예: 10분 TTL) 우선 사용 → 공공데이터 API 일 트래픽 제한(개발계정 500회) 대응
- 전체 17개 도시 데이터는 백엔드에서 배치로 주기 수집 후 서빙하는 방식 권장 (7절 참조)

---

## 7. 화면 구성 (UI 구조 설명)

```
┌─────────────────────────────────────────┐
│  헤더: 서비스 로고 + 타이틀                    │
├─────────────────────────────────────────┤
│  [★즐겨찾기 도시 칩들]                        │
│  [서울][부산][대구][인천][광주]...(17개 칩)     │
├─────────────────────────────────────────┤
│  선택 도시: 서울   측정시각: 14:00 기준         │
│                                           │
│   ┌────────┐  ┌────────┐                │
│   │ PM10   │  │ PM2.5  │   [종합등급 배지] │
│   │ 32㎍/㎥ │  │ 18㎍/㎥ │                │
│   │ 보통    │  │ 좋음    │                │
│   └────────┘  └────────┘                │
│                                           │
│  [오늘 예보] [내일 예보] [모레 예보]           │
│                                           │
│  건강 행동요령: "실외활동 시 특별한 제한이       │
│  필요하지 않습니다."                          │
└─────────────────────────────────────────┘
```
- 모바일: 칩 리스트는 가로 스크롤, 상세 패널은 칩 아래 세로 배치
- PC: 칩 리스트는 좌측 또는 상단 그리드, 상세 패널은 우측 큰 영역

---

## 8. API 연동 요구사항

### 8.1 사용 API 상세
| 구분 | 내용 |
|---|---|
| API명 | 한국환경공단_에어코리아_대기오염정보 |
| 주요 오퍼레이션 | `getCtprvnRltmMesureDnsty` (시도별 실시간 측정정보 조회) |
| 예보 오퍼레이션 | `getMinuDustFrcstDspth` (대기질 예보통보 조회) |
| 요청 방식 | GET, REST (XML/JSON 선택 가능) |
| 필수 파라미터 | serviceKey, returnType(json), numOfRows, pageNo, sidoName, ver |
| 응답 주요 필드 | pm10Value, pm25Value, khaiGrade(통합대기환경지수 등급), dataTime |
| 갱신 주기(원천) | 시간당 1회 (매시 정각 기준 측정소 데이터 갱신) |

### 8.2 연동 아키텍처 권장안
```
[에어코리아 공공데이터 API]
        │  (서버에서 1시간 주기 배치 호출, 17개 시도)
        ▼
[백엔드 서버 / Serverless Function]
        │  (DB 또는 캐시에 저장: Redis/파일/DB)
        ▼
[프론트엔드]  ← 자체 API로 조회 (공공 API 키 노출 방지)
```
- **보안**: 서비스키는 반드시 백엔드/서버리스 함수에서만 사용, 프론트엔드에 노출 금지
- **이유**: 개발계정 일 트래픽 500회 제한 → 사용자 요청마다 직접 호출 시 즉시 소진. 서버에서 시간당 1회씩 17개 도시를 미리 수집해두는 방식이 필수

### 8.3 예외 처리
- 특정 시/도 측정값 결측 시: 최근 유효값 + "최근 업데이트: OO시" 표시 또는 "데이터 없음" 명시
- API 자체 장애 시: 마지막 성공 캐시 데이터 + "갱신 지연 중" 안내 배지

---

## 9. 데이터 표시 기준

### 9.1 통합대기환경지수(CAI) 등급 기준
| 등급 | PM10 (㎍/㎥, 24hr) | PM2.5 (㎍/㎥, 24hr) | 색상(권장) |
|---|---|---|---|
| 좋음 | 0~30 | 0~15 | Blue (#4A90D9) |
| 보통 | 31~80 | 16~35 | Green (#5CB85C) |
| 나쁨 | 81~150 | 36~75 | Orange (#F0AD4E) |
| 매우나쁨 | 151 이상 | 76 이상 | Red (#D9534F) |

### 9.2 등급별 건강 행동요령 (예시 텍스트, 환경부 기준 참고)
- **좋음**: 실외활동에 제한이 필요하지 않음
- **보통**: 민감군은 장시간·무리한 실외활동 다소 제한
- **나쁨**: 민감군은 실외활동 제한, 일반인은 장시간 실외활동 자제
- **매우나쁨**: 민감군은 실외활동 금지, 일반인도 가급적 실외활동 자제 및 마스크 착용

---

## 10. 비기능 요구사항 (NFR)

| 구분 | 요구사항 |
|---|---|
| 성능 | 칩 클릭 후 캐시 데이터 기준 500ms 이내 표시 |
| 반응형 | 360px(모바일) ~ 1920px(데스크톱) 대응 |
| 브라우저 호환성 | 최신 Chrome, Safari, Edge (2개 최신 버전) |
| 접근성 | 색상 등급에 텍스트 라벨 병기(색맹 사용자 고려), 키보드 탭 이동 가능 |
| 가용성 | 원천 API 장애 시에도 캐시 기반 서비스 유지 |
| 데이터 신선도 | 최대 1시간 이내 데이터로 갱신 |

---

## 11. 기술 스택 제안 (참고용)

| 영역 | 제안 |
|---|---|
| 프론트엔드 | React 또는 Next.js (반응형 웹, SSR로 초기 로딩 최적화) |
| 스타일링 | Tailwind CSS (등급별 색상 유틸리티 관리 용이) |
| 백엔드 | Next.js API Route 또는 별도 Node.js 서버 (Express) — 공공 API 프록시 겸 캐시 서버 |
| 캐시/저장 | Redis(권장) 또는 간단히 서버 메모리/JSON 파일 (개인 프로젝트 규모 기준) |
| 배치 스케줄러 | node-cron 또는 Vercel Cron Job (시간당 1회 17개 도시 수집) |
| 배포 | Vercel(프론트+API 통합 배포 용이) |
| 즐겨찾기 저장 | 브라우저 localStorage |
| 차트 라이브러리 | Chart.js (향후 확장 기능인 시간대별 추이 그래프 구현용) |

---

## 12. 마일스톤 (개인 프로젝트 기준 제안)

| 단계 | 내용 | 기간(예시) |
|---|---|---|
| Phase 1 | 공공데이터포털 API 키 발급, API 연동 테스트(Postman) | 2~3일 |
| Phase 2 | 백엔드: 배치 수집 + 캐시 서버 구축 | 3~4일 |
| Phase 3 | 프론트: 칩 리스트 + 상세 패널 UI 구현 | 4~5일 |
| Phase 4 | 즐겨찾기, 에러/로딩 처리, 반응형 대응 | 2~3일 |
| Phase 5 | QA, 성능 최적화, 배포, 포트폴리오 문서화 | 2~3일 |

---

## 13. 리스크 및 대응 방안

| 리스크 | 영향 | 대응 방안 |
|---|---|---|
| 공공데이터 API 트래픽 제한(개발계정 500/일) 초과 | 서비스 데이터 갱신 중단 | 서버 배치 수집 + 캐싱으로 사용자 요청과 API 호출 분리, 운영계정 신청 검토 |
| API 응답 형식/필드 변경 | 파싱 오류 | 응답 스키마 검증 로직 및 예외 처리 추가 |
| 측정소 점검으로 인한 결측치 | 잘못된 정보 전달 | 결측 시 명시적 "데이터 없음" 표기, 0과 구분 |
| 색상만으로 등급 구분 시 접근성 저하 | 색맹 사용자 불편 | 텍스트 라벨 병기 필수 |

---

## 14. 향후 확장 아이디어 (Post v1.0)

- 지도 기반 시각화(전국 히트맵)
- 시/군/구 단위 세분화 및 GPS 기반 "내 위치" 자동 조회
- 웹 푸시 알림("나쁨" 등급 진입 시 알림)
- 시간대별 추이 그래프(최근 24시간, Chart.js 활용)
- PWA 전환으로 홈 화면 추가 지원

---

## 15. 용어 정리

- **PM10**: 입자 지름 10㎛ 이하 미세먼지
- **PM2.5**: 입자 지름 2.5㎛ 이하 초미세먼지
- **CAI (통합대기환경지수)**: 여러 대기오염물질 농도를 통합해 산출한 지수, 4단계 등급으로 표현
- **에어코리아**: 한국환경공단이 운영하는 국가 대기질 정보 공개 플랫폼
